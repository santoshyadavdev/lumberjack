import { LumberjackTimeService } from './index';

describe('@ngworker/lumberjack/testing', () => {
  it(`exposes ${LumberjackTimeService.name}`, () => {
    expect(LumberjackTimeService).toBeDefined();
  });
});
